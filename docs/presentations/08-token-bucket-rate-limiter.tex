\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 8: Token Bucket Rate Limiting}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{The Token Bucket Algorithm}
  \begin{center}
  \resizebox{0.85\linewidth}{!}{
  \begin{tikzpicture}[node distance=1cm and 1.5cm]
    \node[primarycard] (refill) {\textbf{Continuous Inflow}\\$+$ \texttt{rate} tokens/sec};
    \node[card, right=of refill] (bucket) {\textbf{Bucket (Capacity $C$)}\\\texttt{tokens} $\le C$};
    \node[successcard, right=of bucket] (req) {\textbf{Outbound Request}\\Consumes 1 Token};

    \draw[flowarrow] (refill) -- (bucket);
    \draw[accentarrow] (bucket) -- node[above, font=\scriptsize\bfseries] {Wait / Drain} (req);
  \end{tikzpicture}
  }
  \end{center}
  \vspace{0.4cm}
  $$\text{tokens}(t) = \min\Big(\text{capacity},\ \text{tokens}(t_{last}) + (t - t_{last}) \times \text{rate}\Big)$$
\end{frame}

\begin{frame}[fragile]{\texttt{RateLimiter} Implementation}
\begin{lstlisting}[style=nexuspython]
class RateLimiter:
    def __init__(self, rate: float):
        self.rate = rate
        self.capacity = max(1.0, rate)
        self.tokens = self.capacity
        self.last_update = time.time()

    def wait(self) -> None:
        if self.rate <= 0: return
        while True:
            now = time.time()
            elapsed = now - self.last_update
            self.tokens = min(self.capacity, self.tokens + elapsed * self.rate)
            self.last_update = now
            if self.tokens >= 1.0:
                self.tokens -= 1.0; return
            time.sleep(max(0.01, (1.0 - self.tokens) / self.rate))
\end{lstlisting}
\end{frame}

\end{document}

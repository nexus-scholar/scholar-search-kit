\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 18: File I/O, Exporters \& Importers}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{Ecosystem Interoperability}
  \begin{center}
  \resizebox{0.9\linewidth}{!}{
  \begin{tikzpicture}[node distance=0.8cm and 1.5cm]
    \node[primarycard] (importer) {\textbf{Importers}\\RIS $\cdot$ JSON $\cdot$ JSONL};
    
    \node[card, right=of importer] (core) {\textbf{\texttt{Document}} Flow\\Verify $\cdot$ Dedup $\cdot$ Enrich};
    
    \node[successcard, right=of core] (exporter) {\textbf{Exporters}\\JSON (Array) $\cdot$ CSV $\cdot$ BibTeX};

    \draw[flowarrow] (importer) -- (core);
    \draw[accentarrow] (core) -- (exporter);
  \end{tikzpicture}
  }
  \end{center}
\end{frame}

\begin{frame}[fragile]{Exporting & Importing API}
\begin{lstlisting}[style=nexuspython]
# 1. Exporting structured JSON for scholar-pdf-kit
exporter = Exporter()
exporter.json(documents, "dataset.json")
exporter.csv(documents, "summary.csv")

# 2. Ingesting & Verifying Legacy RIS files
importer = RISImporter()
imported_docs = importer.import_file("citations.ris")
\end{lstlisting}
\end{frame}

\end{document}

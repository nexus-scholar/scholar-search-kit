\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 10: Query Lexing \& Translation}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{Query Translation Dialects}
  \begin{center}
  \resizebox{0.9\linewidth}{!}{
  \begin{tikzpicture}[node distance=0.8cm and 1.5cm]
    \node[primarycard] (input) {\textbf{Generic Query}\\`\texttt{title:"deep learning" AND (vision OR audio)}'};
    
    \node[card, right=of input] (lexer) {\textbf{\texttt{QueryParser}}\\Token stream with fields,\\phrases, and operators};
    
    \node[card, above right=0.3cm and 1.2cm of lexer] (s2) {\textbf{S2 Bulk Syntax}\\\texttt{deep learning + (vision | audio)}};
    \node[card, right=1.2cm of lexer] (arxiv) {\textbf{arXiv Query}\\\texttt{ti:"deep learning" AND ...}};
    \node[card, below right=0.3cm and 1.2cm of lexer] (openalex) {\textbf{OpenAlex}\\\texttt{search=... \& filter=...}};

    \draw[flowarrow] (input) -- (lexer);
    \draw[accentarrow] (lexer.east) -- (s2.west);
    \draw[accentarrow] (lexer.east) -- (arxiv.west);
    \draw[accentarrow] (lexer.east) -- (openalex.west);
  \end{tikzpicture}
  }
  \end{center}
\end{frame}

\begin{frame}[fragile]{Tokenizer Contract}
\begin{lstlisting}[style=nexuspython]
@dataclass
class QueryToken:
    value: str
    field: QueryField = QueryField.ALL
    is_phrase: bool = False
    is_operator: bool = False

class QueryParser:
    def parse(self, text: str) -> list[QueryToken]:
        # Extracts quoted phrases, fields (title:, author:),
        # parentheses, and Boolean operators (AND, OR, NOT)
        ...
\end{lstlisting}
\end{frame}

\end{document}

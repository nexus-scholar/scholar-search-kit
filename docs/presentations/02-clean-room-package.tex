\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 2: Clean-Room Package Architecture}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{Modern Python Packaging with \texttt{uv}}
  \begin{center}
  \resizebox{0.85\linewidth}{!}{
  \begin{tikzpicture}[node distance=0.8cm and 1.2cm]
    \node[primarycard] (toml) {\textbf{\texttt{pyproject.toml}}\\PEP 621 Standard};
    \node[card, right=of toml] (src) {\textbf{\texttt{src/} Layout}\\\texttt{src/scholar\_search/}};
    \node[successcard, right=of src] (uv) {\textbf{Isolated Environment}\\\texttt{uv} Virtualenv $\cdot$ Caching};

    \draw[flowarrow] (toml) -- (src);
    \draw[accentarrow] (src) -- (uv);
  \end{tikzpicture}
  }
  \end{center}
  \vspace{0.4cm}
  \begin{itemize}
    \item \textbf{Strict \texttt{src/} Layout}: Prevents accidental imports from development root.
    \item \textbf{Type Annotations}: Built-in type hints for IDE autocomplete and safety.
    \item \textbf{Minimal Dependencies}: Clean modular footprint.
  \end{itemize}
\end{frame}

\begin{frame}[fragile]{\texttt{pyproject.toml} Specification}
\begin{lstlisting}[style=nexuspython]
[project]
name = "scholar-search-kit"
version = "0.1.0"
description = "Federated scientific literature discovery & verification"
dependencies = [
    "pydantic>=2.0.0",
    "requests>=2.31.0",
    "requests-cache>=1.2.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
]

[project.scripts]
scholar-search = "scholar_search.cli:app"
\end{lstlisting}
\end{frame}

\end{document}

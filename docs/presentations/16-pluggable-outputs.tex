\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 16: Deduplication \& Metadata Merging}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{2-Phase Conservative Matching}
  \begin{center}
  \resizebox{0.9\linewidth}{!}{
  \begin{tikzpicture}[node distance=0.8cm and 1.5cm]
    \node[primarycard] (in) {\textbf{Aggregated Documents}};
    
    \node[card, right=of in] (p1) {\textbf{Phase 1: Persistent ID Index}\\Exact Match on DOI, arXiv, PMID};
    \node[card, right=of p1] (p2) {\textbf{Phase 2: Fuzzy Title Matching}\\Normalized String Ratio $\ge 0.95$\\Year Gap Gating $|y_1 - y_2| \le 1$};
    
    \node[successcard, below=0.8cm of p2] (merge) {\textbf{Smart Metadata Synthesis}\\Merge abstracts, venues, authors, MeSH terms};

    \draw[flowarrow] (in) -- (p1);
    \draw[accentarrow] (p1) -- node[above, font=\scriptsize\bfseries] {Unmatched} (p2);
    \draw[flowarrow] (p2) -- (merge);
  \end{tikzpicture}
  }
  \end{center}
\end{frame}

\begin{frame}[fragile]{\texttt{Deduplicator} in Action}
\begin{lstlisting}[style=nexuspython]
deduplicator = Deduplicator()
clusters = deduplicator.deduplicate(documents)

for cluster in clusters:
    rep = cluster.representative
    print(f"Cluster {cluster.cluster_id}: {rep.title} "
          f"({len(cluster.members)} sources, Conf: {cluster.confidence})")
\end{lstlisting}
\end{frame}

\end{document}

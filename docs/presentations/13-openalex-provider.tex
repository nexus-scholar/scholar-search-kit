\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 13: Ingesting OpenAlex at Scale}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{Cursor Pagination \& Inverted Index}
  \begin{center}
  \resizebox{0.85\linewidth}{!}{
  \begin{tikzpicture}[node distance=0.8cm and 1.2cm]
    \node[primarycard] (cursor) {\textbf{Cursor Pagination}\\\texttt{cursor=*} $\rightarrow$ \texttt{next\_cursor}};
    \node[card, right=of cursor] (index) {\textbf{Inverted Index}\\\texttt{\{"Deep": [0], "learning": [1]\}}};
    \node[successcard, right=of index] (prose) {\textbf{Decompressed Abstract}\\"Deep learning..."};

    \draw[flowarrow] (cursor) -- (index);
    \draw[accentarrow] (index) -- (prose);
  \end{tikzpicture}
  }
  \end{center}
\end{frame}

\begin{frame}[fragile]{Abstract Reassembly}
\begin{lstlisting}[style=nexuspython]
def _extract_abstract(self, raw: dict) -> str | None:
    inverted_index = raw.get("abstract_inverted_index")
    if not inverted_index: return None
    word_positions = []
    for word, positions in inverted_index.items():
        for pos in positions:
            word_positions.append((pos, word))
    word_positions.sort(key=lambda x: x[0])
    return " ".join(word for _, word in word_positions)[:5000]
\end{lstlisting}
\end{frame}

\end{document}

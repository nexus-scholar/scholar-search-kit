\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}
\usepackage{nexus_theme}

\title{Building a Reproducible Scholarly Search Toolkit}
\subtitle{Episode 4: The Canonical \texttt{Document} Model}
\author{Nexus Scholar Suite}
\date{}

\begin{document}

\begin{frame}[plain]
  \titlepage
\end{frame}

\begin{frame}{The Normalization Boundary}
  \begin{center}
  \resizebox{0.85\linewidth}{!}{
  \begin{tikzpicture}[node distance=0.8cm and 1.2cm]
    \node[card] (raw1) {OpenAlex JSON};
    \node[card, below=of raw1] (raw2) {PubMed XML};
    \node[card, below=of raw2] (raw3) {Crossref JSON};
    \node[card, below=of raw3] (raw4) {arXiv Atom XML};

    \node[primarycard, right=of raw2, minimum height=3.5cm, align=center] (boundary) {\textbf{Normalization}\\\textbf{Boundary}\\Field Parsers};
    
    \node[successcard, right=of boundary, minimum height=3.5cm, align=center] (doc) {\textbf{Canonical \texttt{Document}}\\Title, Authors, Year, IDs\\MeSH Terms, Citations, Provenance};

    \draw[flowarrow] (raw1.east) -- (boundary.west);
    \draw[flowarrow] (raw2.east) -- (boundary.west);
    \draw[flowarrow] (raw3.east) -- (boundary.west);
    \draw[flowarrow] (raw4.east) -- (boundary.west);
    \draw[accentarrow] (boundary.east) -- (doc.west);
  \end{tikzpicture}
  }
  \end{center}
\end{frame}

\begin{frame}[fragile]{The \texttt{Document} Dataclass}
\begin{lstlisting}[style=nexuspython]
@dataclass
class Document:
    title: str
    year: int | None = None
    provider: str = "unknown"
    provider_id: str = ""
    external_ids: ExternalIds = field(default_factory=ExternalIds)
    abstract: str | None = None
    authors: list[Author] = field(default_factory=list)
    venue: str | None = None
    citations_count: int | None = None
    mesh_terms: list[str] = field(default_factory=list)
    tldr: str | None = None
    query_id: str | None = None
    retrieved_at: datetime | None = None
\end{lstlisting}
\end{frame}

\begin{frame}{Provenance \& Audit Guarantees}
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \begin{block}{Scientific Traceability}
        \begin{itemize}
          \item \texttt{provider\_id}: Primary key in remote repository.
          \item \texttt{query\_id}: Links to exact search instrument.
          \item \texttt{retrieved\_at}: ISO UTC timestamp.
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{block}{Enriched Metadata}
        \begin{itemize}
          \item Medical Subject Headings (\texttt{mesh\_terms}).
          \item AI Abstract Summaries (\texttt{tldr}).
          \item Citation Graph Counts (\texttt{citations\_count}).
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}
\end{frame}

\end{document}
